<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DailyDigest" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">#98a7c2</item>
    </style>
    
    <style name="Base.Theme.DailyDigest.BackgroundImage">
<!--        <item name="android:windowBackground">@drawable/bg_journal_1</item>-->

    </style>

    <!-- Splash Screen theme.  -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <!-- Customize your splash screen theme here -->
        <item name="windowSplashScreenBackground">@color/custom_corner_radius_app_theme_50dp</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.App.DailyDigest</item>
    </style>

    <style name="Theme.App.DailyDigest" parent="Base.Theme.DailyDigest" />
    <style name="Theme.DailyDigest" parent="Base.Theme.DailyDigest.BackgroundImage" />

<!--    Rating bar star theme-->
    <style name="MyRatingBar" parent="Widget.AppCompat.RatingBar">

        <!-- normal color of star-->
        <item name="colorControlNormal">#66D2DDE1</item>

        <!-- activated color of star-->
        <item name="colorControlActivated">#98a7c2</item>
    </style>
</resources>