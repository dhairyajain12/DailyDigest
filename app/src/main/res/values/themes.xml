<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DailyDigest" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Splash Screen theme.  -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <!-- Customize your splash screen theme here -->
        <item name="windowSplashScreenBackground">@color/custom_app_theme</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.DailyDigest</item>
    </style>

    <style name="Theme.DailyDigest" parent="Base.Theme.DailyDigest" />
</resources>